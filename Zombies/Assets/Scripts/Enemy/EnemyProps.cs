using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyProps : MonoBehaviour
{


    public bool didMultiply;

    // Start is called before the first frame update
    void Start()
    {
        didMultiply = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
